<!--
     Default Manifest for the ohmageApp wrapper apk. If built with ant, the versionCode
     and versionName will be replaced with the version of ohmageAndroidLib if they are left as 0
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.ohmage.mobilizingcs"
    android:versionCode="14082600"
    android:versionName="0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <application
        android:name="MobilizeApplication"
        android:allowBackup="true"
        android:icon="@drawable/mobilize_icon"
        android:label="@string/app_name"
        android:theme="@style/OhmageTheme" >
        <activity
            android:name="org.ohmage.activity.OhmageLauncher"
            android:theme="@style/OhmageTheme.LogoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
